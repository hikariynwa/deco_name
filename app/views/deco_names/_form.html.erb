<%= form_with(model: deco_name) do |form| %>
  <% if deco_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(deco_name.errors.count, "error") %> prohibited this deco_name from being saved:</h2>

      <ul>
        <% deco_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
